name: adr_mark_accepted
description: Move an ADR from proposed to accepted status
inputSchema:
  type: object
  additionalProperties: false
  properties:
    adr_id:
      type: string
      description: ADR ID to accept (e.g. "adr-001")
  required: [adr_id]
